## StackHeaderToolbarMenuItemShowAsActionAndroid {#stackheadertoolbarmenuitemshowasactionandroid}

> **StackHeaderToolbarMenuItemShowAsActionAndroid** = `"always"` \| `"alwaysWithText"` \| `"ifRoom"` \| `"ifRoomWithText"` \| `"never"`

Defined in: [components/stack/header/StackHeaderConfig.android.types.ts:185](https://github.com/software-mansion/react-native-screens/blob/8de39c66b032cbe0b0997f370555ded4ff950eab/src/components/stack/header/StackHeaderConfig.android.types.ts#L185)

Specifies whether a menu element is displayed as a button in the Toolbar.

The following values are supported:
- `'always'` — always displays the element as a button in the Toolbar.
- `'alwaysWithText'` — always displays the element as a button in the
  Toolbar, forcing the text label to be visible even if an icon is provided.
- `'ifRoom'` — displays the element as a button in the Toolbar only if the
  system determines there is sufficient space.
- `'ifRoomWithText'` — displays the element as a button in the Toolbar if the
  system determines there is sufficient space, forcing the text label to be
  visible even if an icon is provided.
- `'never'` — never displays the element as a button in the Toolbar; it will
  be placed in the overflow menu instead.

### Platform

android

### Since

5.0
