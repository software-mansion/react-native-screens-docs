## ScrollEdgeEffect {#scrolledgeeffect}

> **ScrollEdgeEffect** = `"automatic"` \| `"hard"` \| `"soft"` \| `"hidden"`

Defined in: [components/shared/types.ts:103](https://github.com/software-mansion/react-native-screens/blob/8de39c66b032cbe0b0997f370555ded4ff950eab/src/components/shared/types.ts#L103)

Visual effect applied at a scrollable edge.

The following values are supported:
- `'automatic'` — the system default effect for the edge.
- `'hard'` — a hard cut-off effect.
- `'soft'` — a soft, fading effect.
- `'hidden'` — no edge effect.

### Platform

ios

### Since

5.0
