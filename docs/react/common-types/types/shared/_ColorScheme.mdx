## ColorScheme {#colorscheme}

> **ColorScheme** = `"light"` \| `"dark"`

Defined in: [components/shared/types.ts:51](https://github.com/software-mansion/react-native-screens/blob/8de39c66b032cbe0b0997f370555ded4ff950eab/src/components/shared/types.ts#L51)

Interface style applied to a container's content.

The following values are supported:
- `'light'` — the light interface style.
- `'dark'` — the dark interface style.

### Platform

android, ios

### Since

5.0
