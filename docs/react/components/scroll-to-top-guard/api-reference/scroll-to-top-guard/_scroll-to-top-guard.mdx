## ScrollToTopGuard {#scroll-to-top-guard}

Defined in: [components/scroll-to-top-guard/ScrollToTopGuard.types.ts:17](https://github.com/software-mansion/react-native-screens/blob/8de39c66b032cbe0b0997f370555ded4ff950eab/src/components/scroll-to-top-guard/ScrollToTopGuard.types.ts#L17)

**`Experimental`**

Workaround component for a `react-native` scroll-to-top issue on iOS.

### Remarks

This component is meant to be used only as a temporary workaround until the
issue is fixed on `react-native`'s side — see the
[issue report](https://github.com/facebook/react-native/issues/56061).

Falls back to `React.Fragment` on platforms other than iOS.

### Platform

ios

### Since

5.0

### Extends

- `ViewProps`
