---
title: TabsScreenAppearanceAndroid
sidebar_label: TabsScreenAppearanceAndroid
---

import { Badges } from '@swmansion/t-rex-ui';

# TabsScreenAppearanceAndroid

Used by [`TabsScreenPropsAndroid`](./TabsScreenPropsAndroid).

## Fields

<Badges platforms={['android']}>
### disabled
</Badges>

Defines the colors for tab bar items when they are disabled.

```ts
disabled?: TabsScreenItemStateAppearanceAndroid;
```

The color scheme is determined by the configuration of the currently selected tab.
Maps to Android `state_enabled=false`.

See [`TabsScreenItemStateAppearanceAndroid`](./TabsScreenItemStateAppearanceAndroid) for full type definition.

---

<Badges platforms={['android']}>
### focused
</Badges>

Defines the colors for a tab bar item when it receives focus.

```ts
focused?: TabsScreenItemStateAppearanceAndroid;
```

The color scheme is determined by the configuration of the currently selected tab.
Maps to Android `state_focused=true` (Used mostly for keyboard navigation).

See [`TabsScreenItemStateAppearanceAndroid`](./TabsScreenItemStateAppearanceAndroid) for full type definition.

---

<Badges platforms={['android']}>
### normal
</Badges>

Defines the colors for all tab bar items which are in their enabled, unselected and unfocused state.

```ts
normal?: TabsScreenItemStateAppearanceAndroid;
```

The color scheme is determined by the configuration of the currently selected tab.

See [`TabsScreenItemStateAppearanceAndroid`](./TabsScreenItemStateAppearanceAndroid) for full type definition.

---

<Badges platforms={['android']}>
### selected
</Badges>

Defines the colors for the tab bar item that is currently active.

```ts
selected?: TabsScreenItemStateAppearanceAndroid;
```

The color scheme is determined by the configuration of the currently selected tab itself.
Maps to Android `state_selected=true`.

See [`TabsScreenItemStateAppearanceAndroid`](./TabsScreenItemStateAppearanceAndroid) for full type definition.

---

<Badges platforms={['android']}>
### tabBarBackgroundColor
</Badges>

Specifies the background color for the entire tab bar.

```ts
tabBarBackgroundColor?: ColorValue;
```

---

<Badges platforms={['android']}>
### tabBarItemActiveIndicatorColor
</Badges>

Specifies the background color of the active indicator.

```ts
tabBarItemActiveIndicatorColor?: ColorValue;
```

---

<Badges platforms={['android']}>
### tabBarItemActiveIndicatorEnabled
</Badges>

Specifies if the active indicator should be used.

```ts
tabBarItemActiveIndicatorEnabled?: boolean;
```

**Default:** `true`

---

<Badges platforms={['android']}>
### tabBarItemBadgeBackgroundColor
</Badges>

Specifies the background color of the badge.

```ts
tabBarItemBadgeBackgroundColor?: ColorValue;
```

---

<Badges platforms={['android']}>
### tabBarItemBadgeTextColor
</Badges>

Specifies the text color of the badge.

```ts
tabBarItemBadgeTextColor?: ColorValue;
```

---

<Badges platforms={['android']}>
### tabBarItemLabelVisibilityMode
</Badges>

Specifies the label visibility mode.

```ts
tabBarItemLabelVisibilityMode?: TabBarItemLabelVisibilityMode;
```

The label visibility mode defines when the labels of each item bar should be displayed.

The following values are available:
- `auto` - the label behaves as in “labeled” mode when there are 3 items or less, or as in “selected” mode when there are 4 items or more
- `selected` - the label is only shown on the selected navigation item
- `labeled` - the label is shown on all navigation items
- `unlabeled` - the label is hidden for all navigation items

The supported values correspond to the official Material Components documentation.

**Default:** `auto`

---

<Badges platforms={['android']}>
### tabBarItemRippleColor
</Badges>

Specifies the color of each tab bar item's ripple effect.

```ts
tabBarItemRippleColor?: ColorValue;
```

---

<Badges platforms={['android']}>
### tabBarItemTitleFontFamily
</Badges>

Specifies the font family used for the title of each tab bar item.

```ts
tabBarItemTitleFontFamily?: string;
```

---

<Badges platforms={['android']}>
### tabBarItemTitleFontStyle
</Badges>

Specifies the font style used for the title of each tab bar item.

```ts
tabBarItemTitleFontStyle?: 'normal' | 'italic';
```

---

<Badges platforms={['android']}>
### tabBarItemTitleFontWeight
</Badges>

Specifies the font weight used for the title of each tab bar item.

```ts
tabBarItemTitleFontWeight?: 
  | 'light'
  | 'regular'
  | 'medium'
  | 'bold'
  | 'normal'
  | '100'
  | '200'
  | '300'
  | '400'
  | '500'
  | '600'
  | '700'
  | '800'
  | '900'
  | 100
  | 200
  | 300
  | 400
  | 500
  | 600
  | 700
  | 800
  | 900
  | 'ultralight'
  | 'thin'
  | 'semibold'
  | 'condensedBold'
  | 'condensed'
  | 'heavy'
  | 'black';
```

---

<Badges platforms={['android']}>
### tabBarItemTitleLargeLabelFontSize
</Badges>

Specifies the font size used for the title of selected tab bar item.

```ts
tabBarItemTitleLargeLabelFontSize?: number;
```

The size is represented in scale-independent pixels (sp).

---

<Badges platforms={['android']}>
### tabBarItemTitleSmallLabelFontSize
</Badges>

Specifies the font size used for the title of unselected tab bar items.

```ts
tabBarItemTitleSmallLabelFontSize?: number;
```

The size is represented in scale-independent pixels (sp).

---
